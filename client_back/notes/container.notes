Container
-> Seed
-> NetworkParams
-> Exposure?

-> generate the HD wallet
-> ask the wallet to give me a node

container.wallet.node(0).scripthash
container.node(0)

account.wallet?

container.eachWallet(WalletExposure.internal) |wallet| {
  wallet.doSomething()
}

"My Wallet" <- name? "Accounts" or List<Account>
  Account ("Debit Card")
  -> is an HDWallet OR is Just a Private Key

